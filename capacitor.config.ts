import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tasks.project.app',
  appName: 'project-tasks',
  webDir: 'dist/project-tasks',
  bundledWebRuntime: false
};

export default config;
